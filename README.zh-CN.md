# React OpenSpec Flow Starter 🌊

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![OpenSpec](https://img.shields.io/badge/Spec-OpenSpec-blue)](https://github.com/openspec)
[![SDD](https://img.shields.io/badge/Methodology-SDD-green)](https://en.wikipedia.org/wiki/Specification_by_example)

> [中文说明](./README.zh-CN.md) | **[English](./README.md)**

**基于 OpenSpec 的 React SDD (规范驱动开发) 范例。**
本项目整合了 **OpenSpec 开源工具链**，通过标准化的定义与校验，为 Cursor、Claude Code 等 AI 工具提供精确、无歧义的上下文，实现高质量的、受约束的 AI 辅助开发。


## 📖 简介

**React OpenSpec Flow Starter** 的核心理念是：**在 AI 时代，代码是廉价的，但架构约束是昂贵的。**

目前的 AI 编程助手（如 Cursor, Claude Code）在处理大型项目时，常因缺乏全局上下文而逐渐产生“幻觉”或偏离架构。本项目通过将 **OpenSpec** 确立为**真理来源 (Single Source of Truth)**，并深度集成 OpenSpec 工具来解决这一痛点。

我们的工作流非常明确：**人类利用 OpenSpec 工具定义并校验“是什么”，AI 负责在 React 中实现“怎么做”。**

## 🚀 核心特性

- **🛠️ OpenSpec 工具链原生集成**
  预置 OpenSpec CLI 配置，支持对 `specs/` 目录下的定义文件进行**语法校验 (Linting)**、**类型验证**及**可视化预览**。确保喂给 AI 的“上下文”本身是逻辑自洽的。

- **🤖 AI 上下文锚定 (Context Anchoring)**
  通过内置的 `.cursorrules` 文件和提示策略，强制 AI 工具在生成代码前必须先读取并理解 `specs/` 目录下的定义。

- **🛡️ 基于约束的生成**
  将 AI 的生成能力限制在 OpenSpec 定义的范围内。如果 Spec 里标记字段为 `readonly`，AI 生成的表单组件就必须是禁用的——拒绝随意发挥。

## 🔄 OpenSpec 标准工作流 (The Flow)

本项目严格遵循 OpenSpec 定义的四步开发闭环，确保从思维到代码的无损传输：

```text
┌──────────────────────┐
│ 1. 起草提案 (Draft)  │ ✏️ 人类：在 specs/ 目录定义需求
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ 2. 审查对齐 (Align)  │ 🔍 工具：运行 openspec lint 确保逻辑自洽
└──────────┬───────────┘    (AI 上下文锚定点)
           │
           ▼
┌──────────────────────┐
│ 3. 实现任务 (Impl)   │ 🤖 AI：Cursor/Claude 读取 Spec 生成代码
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ 4. 归档更新 (Update) │ 📚 系统：Spec 即文档，作为新功能的基石
└──────────────────────┘
```

```mermaid
graph TD
    A[1. 起草提案 Draft] -->|明确要做什么| B(2. 审查对齐 Align)
    B -->|人和 AI 达成一致| C(3. 实现任务 Implement)
    C -->|AI 按规范写代码| D[4. 归档更新 Archive]
    D -->|更新项目文档| A
